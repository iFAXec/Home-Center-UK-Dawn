<h1>{{product.title}}</h1>
<p>{{product.price |  money }}</p>

{% schema %}
    {
        "name": "Sample-product",
        "settings": [
        
        ],
        "presets": [
          {
            "name": "Sample Product",
            "settings": {
              
            }
          }
        ],        
        "enabled_on": {
        "templates": ["product"],
        "groups": ["*"]
        }
    }
{% endschema %}
